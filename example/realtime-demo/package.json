{
  "name": "realtime-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "./start-demo.sh",
    "typecheck": "bun ../../sdk/syntrix-client-ts/node_modules/typescript/bin/tsc --noEmit -p tsconfig.json",
    "build": "bun run typecheck && bun build src/main.ts --target browser --format esm --splitting --outdir dist --entry-naming '[name].js' --chunk-naming 'chunks/[name]-[hash].js'"
  },
  "dependencies": {
    "@syntrix/client": "file:../../sdk/syntrix-client-ts"
  }
}
